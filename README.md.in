kanso-jsoneditor
================

jsoneditor wrapped for kan.so

This is currently version @VERSION@.  The jsoneditor website is:

@URL@

Usage
-----

General usage:

```
var $domobject; // div
var jsoneditor = require("jsoneditor");
var editor = new jsoneditor($domobject);
```

Options or more information on the API can be found on the 
jsoneditor website: @URL@.


